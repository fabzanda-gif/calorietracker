From d8ba378c36d3cbb0e779a3f4dd17a02c6790d3ec Mon Sep 17 00:00:00 2001
From: fabzanda-gif <fab.zanda@gmail.com>
Date: Wed, 2 Sep 2026 15:07:16 +0300
Subject: [PATCH 1/2] Fix app pages around desktop sidebar

---
 frontend/app/activities/ActivitiesPage.module.css |  4 +++-
 frontend/app/inventory/InventoryPage.module.css   | 12 ++++++++++++
 frontend/app/profile/ProfilePage.module.css       | 12 ++++++++++++
 frontend/app/progress/ProgressPage.module.css     | 11 +++++++++++
 frontend/app/recipes/RecipesPage.module.css       | 11 +++++++++++
 5 files changed, 49 insertions(+), 1 deletion(-)

diff --git a/frontend/app/activities/ActivitiesPage.module.css b/frontend/app/activities/ActivitiesPage.module.css
index 03e0a70..76d78ec 100644
--- a/frontend/app/activities/ActivitiesPage.module.css
+++ b/frontend/app/activities/ActivitiesPage.module.css
@@ -646,10 +646,12 @@
 
 @media (min-width: 760px) {
   .page {
+    width: min(1180px, calc(100% - 268px));
     margin-left: max(
-      236px,
+      252px,
       calc((100vw - 1180px + 236px) / 2)
     );
+    margin-right: 0;
   }
 }
 
diff --git a/frontend/app/inventory/InventoryPage.module.css b/frontend/app/inventory/InventoryPage.module.css
index f6005cd..2652379 100644
--- a/frontend/app/inventory/InventoryPage.module.css
+++ b/frontend/app/inventory/InventoryPage.module.css
@@ -6,6 +6,18 @@
   color: #153354;
 }
 
+@media (min-width: 760px) {
+  .page {
+    width: min(1180px, calc(100% - 236px));
+    margin-left: max(
+      236px,
+      calc((100vw - 1180px + 236px) / 2)
+    );
+    margin-right: 0;
+    box-sizing: border-box;
+  }
+}
+
 .header {
   display: grid;
   grid-template-columns: 1fr auto auto;
diff --git a/frontend/app/profile/ProfilePage.module.css b/frontend/app/profile/ProfilePage.module.css
index ea962e3..c394e10 100644
--- a/frontend/app/profile/ProfilePage.module.css
+++ b/frontend/app/profile/ProfilePage.module.css
@@ -5,6 +5,18 @@
   margin: 0 auto;
 }
 
+@media (min-width: 760px) {
+  .page {
+    width: min(960px, calc(100% - 236px));
+    margin-left: max(
+      236px,
+      calc((100vw - 960px + 236px) / 2)
+    );
+    margin-right: 0;
+    box-sizing: border-box;
+  }
+}
+
 .header {
   display: flex;
   align-items: flex-end;
diff --git a/frontend/app/progress/ProgressPage.module.css b/frontend/app/progress/ProgressPage.module.css
index e0c544f..39081db 100644
--- a/frontend/app/progress/ProgressPage.module.css
+++ b/frontend/app/progress/ProgressPage.module.css
@@ -609,6 +609,17 @@
   padding: 52px 0 72px;
 }
 
+@media (min-width: 760px) {
+  .page {
+    width: min(1120px, calc(100% - 268px));
+    margin-left: max(
+      252px,
+      calc((100vw - 1120px + 236px) / 2)
+    );
+    margin-right: 0;
+  }
+}
+
 .header {
   margin-bottom: 24px;
 }
diff --git a/frontend/app/recipes/RecipesPage.module.css b/frontend/app/recipes/RecipesPage.module.css
index 9cc4080..90f4157 100644
--- a/frontend/app/recipes/RecipesPage.module.css
+++ b/frontend/app/recipes/RecipesPage.module.css
@@ -256,6 +256,17 @@
   padding: 50px 0 80px;
 }
 
+@media (min-width: 760px) {
+  .page {
+    width: min(1120px, calc(100% - 268px));
+    margin-left: max(
+      252px,
+      calc((100vw - 1120px + 236px) / 2)
+    );
+    margin-right: 0;
+  }
+}
+
 
 /* ==========================================================
    HEADER
-- 
2.51.1


From 1ca1965daa3a3f91707cd16db544d9050da5a74f Mon Sep 17 00:00:00 2001
From: fabzanda-gif <fab.zanda@gmail.com>
Date: Wed, 2 Sep 2026 15:10:02 +0300
Subject: [PATCH 2/2] Add recent meal quick picker

---
 backend/api/routers/meals.py                  |  2 +-
 backend/tests/test_api_meals_history.py       | 24 ++++++-
 frontend/components/home/HomeShell.module.css | 13 ++++
 frontend/components/home/HomeShell.tsx        | 67 +++++++++++++++++++
 frontend/lib/api/meals.ts                     | 16 +++++
 5 files changed, 120 insertions(+), 2 deletions(-)

diff --git a/backend/api/routers/meals.py b/backend/api/routers/meals.py
index 7c3d70a..1537912 100644
--- a/backend/api/routers/meals.py
+++ b/backend/api/routers/meals.py
@@ -274,7 +274,7 @@ def get_meal_history(
     repo: MealsRepository = Depends(get_meals_repository),
 ):
     try:
-        meals = repo.list_history_compatible(current_user.id)
+        meals, _enhanced = repo.list_history_compatible(current_user.id)
 
         return {
             "count": len(meals),
diff --git a/backend/tests/test_api_meals_history.py b/backend/tests/test_api_meals_history.py
index ee32832..f601417 100644
--- a/backend/tests/test_api_meals_history.py
+++ b/backend/tests/test_api_meals_history.py
@@ -1,4 +1,5 @@
-from backend.api.routers.meals import router as meals_router
+from backend.api.dependencies import CurrentUser
+from backend.api.routers.meals import get_meal_history, router as meals_router
 
 
 def test_meals_history_routes_are_registered():
@@ -10,3 +11,24 @@ def test_meals_history_routes_are_registered():
     assert "/meals/history" in paths
     assert "/meals/range" in paths
     assert "/meals/by-type/{meal_type}" in paths
+
+
+def test_meals_history_unpacks_repository_compatibility_result():
+    class FakeRepository:
+        def list_history_compatible(self, user_id):
+            assert user_id == "user-1"
+            return ([{"id": "meal-1", "name": "Pasta"}], True)
+
+    result = get_meal_history(
+        current_user=CurrentUser(
+            id="user-1",
+            access_token="token",
+            metadata={},
+        ),
+        repo=FakeRepository(),
+    )
+
+    assert result == {
+        "count": 1,
+        "items": [{"id": "meal-1", "name": "Pasta"}],
+    }
diff --git a/frontend/components/home/HomeShell.module.css b/frontend/components/home/HomeShell.module.css
index 0f250c4..0a72577 100644
--- a/frontend/components/home/HomeShell.module.css
+++ b/frontend/components/home/HomeShell.module.css
@@ -3637,6 +3637,19 @@
   gap: 8px;
 }
 
+.recentMealPicker {
+  display: grid;
+  gap: 6px;
+}
+
+.recentMealPicker > span {
+  color: #68768a;
+  font-size: 0.68rem;
+  font-weight: 800;
+  letter-spacing: 0.05em;
+  text-transform: uppercase;
+}
+
 .conversationControls select,
 .conversationControls textarea {
   width: 100%;
diff --git a/frontend/components/home/HomeShell.tsx b/frontend/components/home/HomeShell.tsx
index 7e6aec5..d9ea447 100644
--- a/frontend/components/home/HomeShell.tsx
+++ b/frontend/components/home/HomeShell.tsx
@@ -35,6 +35,7 @@ import {
   confirmConversationalMeal,
   deleteMeal,
   getMeal,
+  getMealHistory,
   getMealsForDate,
   previewConversationalMeal,
   previewPhotoMeal,
@@ -394,6 +395,8 @@ export function HomeShell() {
     useState<LoggedMeal | null>(null);
   const [actualMeals, setActualMeals] =
     useState<LoggedMeal[]>([]);
+  const [mealHistory, setMealHistory] =
+    useState<LoggedMeal[]>([]);
 
   const [actualActivities, setActualActivities] =
     useState<Activity[]>([]);
@@ -473,6 +476,27 @@ export function HomeShell() {
     [actualMeals],
   );
 
+  const recentMealChoices = useMemo(() => {
+    const seen = new Set<string>();
+
+    return mealHistory.filter((meal) => {
+      const name = (meal.base_name || meal.name || "").trim();
+
+      if (!name) {
+        return false;
+      }
+
+      const key = `${meal.meal_type.toLowerCase()}::${name.toLowerCase()}`;
+
+      if (seen.has(key)) {
+        return false;
+      }
+
+      seen.add(key);
+      return true;
+    });
+  }, [mealHistory]);
+
   useEffect(() => {
     if (
       conversationPreview ||
@@ -854,6 +878,7 @@ export function HomeShell() {
           weightHistoryPayload,
           dayHistoryPayload,
           profilePayload,
+          mealHistoryPayload,
         ] = await Promise.all([
           getDay(
             date,
@@ -895,6 +920,10 @@ export function HomeShell() {
             accessToken,
           ),
           getProfile(accessToken),
+          getMealHistory(accessToken).catch(() => ({
+            count: 0,
+            items: [],
+          })),
         ]);
 
         const nextMealOptionsPayload =
@@ -921,6 +950,7 @@ export function HomeShell() {
               : null,
           );
           setActualMeals(mealsPayload.items);
+          setMealHistory(mealHistoryPayload.items);
           setActualActivities(
             activitiesPayload.items,
           );
@@ -1251,6 +1281,7 @@ export function HomeShell() {
       nextMealPayload,
       mealsPayload,
       activitiesPayload,
+      mealHistoryPayload,
     ] = await Promise.all([
       getDay(date, accessToken),
       getDayBudget(date, accessToken),
@@ -1266,6 +1297,10 @@ export function HomeShell() {
         date,
         accessToken,
       ),
+      getMealHistory(accessToken).catch(() => ({
+        count: 0,
+        items: [],
+      })),
     ]);
 
     const nextMealOptionsPayload =
@@ -1288,6 +1323,7 @@ export function HomeShell() {
         : null,
     );
     setActualMeals(mealsPayload.items);
+    setMealHistory(mealHistoryPayload.items);
     setActualActivities(
       activitiesPayload.items,
     );
@@ -2546,6 +2582,37 @@ export function HomeShell() {
             </div>
 
             <div className={styles.conversationControls}>
+              {conversationMode === "text" && recentMealChoices.length ? (
+                <label className={styles.recentMealPicker}>
+                  <span>Inserimento rapido</span>
+                  <select
+                    defaultValue=""
+                    aria-label="Scegli un pasto già registrato"
+                    onChange={(event) => {
+                      const index = Number(event.target.value);
+                      const meal = recentMealChoices[index];
+
+                      if (!meal) {
+                        return;
+                      }
+
+                      setConversationMealType(meal.meal_type || "Pranzo");
+                      setConversationText(meal.base_name || meal.name);
+                      setConversationPreview(null);
+                      setConversationError(null);
+                      setConversationSuccess(null);
+                    }}
+                  >
+                    <option value="">Scegli tra i pasti recenti…</option>
+                    {recentMealChoices.map((meal, index) => (
+                      <option key={`${meal.id ?? index}-${meal.meal_type}`} value={index}>
+                        {meal.name} · {meal.meal_type}
+                      </option>
+                    ))}
+                  </select>
+                </label>
+              ) : null}
+
               <select
                 value={conversationMealType}
                 onChange={(event) =>
diff --git a/frontend/lib/api/meals.ts b/frontend/lib/api/meals.ts
index bffbb25..0173e45 100644
--- a/frontend/lib/api/meals.ts
+++ b/frontend/lib/api/meals.ts
@@ -45,6 +45,11 @@ export interface MealsForDateResponse {
   items: LoggedMeal[];
 }
 
+export interface MealHistoryResponse {
+  count: number;
+  items: LoggedMeal[];
+}
+
 export function getMealsForDate(
   dayDate: string,
   accessToken?: string | null,
@@ -57,6 +62,17 @@ export function getMealsForDate(
   );
 }
 
+export function getMealHistory(
+  accessToken?: string | null,
+): Promise<MealHistoryResponse> {
+  return apiRequest<MealHistoryResponse>(
+    "/meals/history",
+    {
+      accessToken,
+    },
+  );
+}
+
 
 export interface StructuredMealCreateIngredient {
   ingredient_id: string;
-- 
2.51.1

